IF(WIN32)
    IF(MSVC)
        SET(CMAKE_SHARED_LINKER_FLAGS_DEBUG "${CMAKE_SHARED_LINKER_FLAGS_DEBUG} /NODEFAULTLIB:MSVCRT")
    ENDIF(MSVC)
ENDIF(WIN32)


SET(TARGET_H
    GeometryUtil.h
	)

SET(TARGET_SRC
	ReaderWriterDRC.cpp
)

SET(TARGET_LIBRARIES_VARS 
	OSG_LIBRARY
	OSGDB_LIBRARY 
	OSGUTIL_LIBRARY 
	OPENTHREADS_LIBRARY
	DRACO_LIBRARY
	)
	
INCLUDE_DIRECTORIES(${GWEARTH_SOURCE_DIR}/Src ${OSG_INCLUDE_DIR} ${DRACO_INCLUDE_DIR})
	
setup_plugin(drc)

SET(LIB_NAME building)
SET(LIB_PUBLIC_HEADERS ${TARGET_H})
INCLUDE(ModuleInstallGwEarthDriverIncludes OPTIONAL)